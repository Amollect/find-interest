#find amount after adding interest

base_a=float(input("enter the original amount:"))
inte_a=float(input("enter interest on base amount"))
amount=(base_a*inte_a)/100
total=amount+base_a
print("amount after adding interset in original amount:",total)